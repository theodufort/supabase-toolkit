import { Config } from "./types/config";

export const appConfig: Config = {
  app: {
    name: "Next.js Build Plate",
    description: "A boilerplate made for vibe coders.",
  },
  auth: {},
  toast_notification: { position: "bottom-right" },
};
